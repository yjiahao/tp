package seedu.address.testutil;

import seedu.address.model.person.Id;

/**
 * A utility class containing a list of {@code Id} objects to be used in tests.
 */
public class TypicalIds {
    public static final Id ID_FIRST = Id.of(1);
    public static final Id ID_SECOND = Id.of(2);
    public static final Id ID_THIRD = Id.of(3);
}
